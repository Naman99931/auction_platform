<%= form_with(model: [@item, @comment], local: true) do |form| %>
  <div>
      <%= form.text_field :content, placeholder: "type comment here" %>
      
  </div>
  <div class="actions">
       <%= form.submit "Comment" %>
  </div>
<% end %>